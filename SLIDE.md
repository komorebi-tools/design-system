# SLIDE.md - スライド固有の規約

> 2026/9/21 に `FIGURE.md` の §5 から切り出した。
> 値は実際のスライドから抽出して測ったもので、確定ではありません。

---

## 0. このファイルの位置づけ

スライド ( Googleスライド ･ PowerPoint ) だけに適用する規約をまとめる。
HTMLレポートと Web には適用しない。

スライドを作るときは `FIGURE.md` と合わせて読む。
ファイルの役割分担は `FIGURE.md` §0 にある。
章番号は `FIGURE.md` にあったときの §5.1 〜 §5.8 のまま使っている。

---

## 5. スライド固有の規約

ここから先はスライドだけに適用する。
HTMLレポートには `DESIGN.md` のタイポグラフィを使う。

### 5.1 タイポグラフィ

| 項目 | 値 |
|---|---|
| 本文の色 | `#434343` |
| 本文のサイズ | 14pt |

#### フォント

| 用途 | フォント名 | 備考 |
|---|---|---|
| 和文 ･ 本文 | `Zen Kaku Gothic New Medium` | 中字。フォント名にそのまま Medium を含める <!-- design-check: allow NO_PERIOD_LINEBREAK --> |
| 欧文のみの語 | `Noto Sans JP` | 日付、Komorebi inc.、色名など |
| 等幅 | `Roboto Mono` | カラーコード、トークン名、桁を揃えて読む数値 |

pptx でウェイトを指定する方法はないので、中字はフォント名を `Zen Kaku Gothic New Medium` と書く。
`Zen Kaku Gothic New` だけだと Regular になる。

等幅は「桁を縦に揃えて比較する場所」と「コードとして読ませる場所」に限る。
グラフの軸ラベルなど、桁を揃える必要がない数値には使わない。

HTML側の等幅は `DESIGN.md` の `SFMono-Regular, Consolas, Menlo` のまま。
Googleスライドにこれらのフォントがないため、スライドだけ `Roboto Mono` を使う。

本文の色は `#434343` のままとし、Text Primary `#333333` に揃えない ( 経緯は `RATIONALE.md` §5.1 )。

#### サイズの段階

`10 / 12 / 14 / 16 / 18 / 20 / 24 pt`

基準は 14pt。
これより小さいサイズは、レイアウト上どうしても入らない場合の例外に限る。
入らないときは文字を小さくするのではなく、要素を減らすか分割する。

グラフの凡例と軸ラベルは例外とし、10pt 以下まで下げてよい。

#### 目安

| 要素 | サイズ |
|---|---|
| スライドタイトル | 18pt |
| ブロックの見出し | 16 〜 20pt ･ 太字 ･ Primary Dark |
| 本文 | 14pt |
| サブラベル ･ hex ･ 数値 | 12pt |
| 下部の注記 | 11pt |

### 5.2 配置

スライドサイズは 10 × 5.625 in ( 16:9 )。

| 要素 | 位置 |
|---|---|
| 小見出し | y 0.40 |
| タイトル | y 0.65 |
| コンテンツエリア | x 0.39 〜 9.60、y 1.20 〜 5.22 |
| 出典 ･ 注記 | y 5.37 |

小見出しとタイトルは、テンプレートのレイアウトに用意されているプレースホルダーに入れる。
新しいテキストボックスを重ねると、空のプレースホルダーが下に残って見出しが二重に見える。
プレースホルダーは x 0.60 から始まる。
コンテンツは x 0.39 から始めるので、見出しとコンテンツで左端は揃わない。
これはテンプレートの仕様。

### 5.3 pill ･ マーカー ･ カード

カテゴリ配色の4値をそのまま使う。

| 素材 | 使う値 |
|---|---|
| pill | カテゴリの**文字色**を面に ･ 白文字 |
| マーカー | 面を不透明度30% ･ 平行四辺形 ･ 文字はカテゴリの文字色を太字 |
| カード ･ タイル | 地 ･ 本文は `#434343` |
| 番号カード | カテゴリの**文字色**を面に ･ 白文字 ･ 番号を大きく、名前を小さく |

#### マーカーのつくり方

1. マーカーは平行四辺形 ( 高さを低くしたもの )。
   面色を不透明度 30% にする
2. 文字の下半分に重ねる ( 上半分は出す )
3. 文字の左右に少しはみ出させる
4. 文字はカテゴリの文字色を太字

1スライドに2〜3箇所まで。
帯を敷かずに文字色だけで強調してもよい。
カテゴリ色のほかに、基調 ( Primary ) のマーカー `#C9D3E4` も使える ( `FIGURE.md` §1.2 )。
カテゴリを割り当てていない資料の強調は、こちらを既定にする。

#### パターン

| パターン | 構成 |
|---|---|
| A ･ 番号カード | 左に文字色ベタの正方形 ( 番号 ＋ 名前 )、右に本文 |
| B ･ タイル一覧 | 地色のタイルに文字色。枠線も影も付けない <!-- design-check: allow NO_PERIOD_LINEBREAK --> |
| C ･ 見出し帯と引用 | 見出し帯は文字色ベタ、カードの地は地色、引用は白のボックスで抜く |
| D ･ pill と矢印 | pill は文字色に白文字。行は本文色のまま <!-- design-check: allow NO_PERIOD_LINEBREAK --> |
| E ･ pill を重ねる | pill をカードの上端に半分かける |

2軸4象限マップを色分けする場合は `FIGURE.md` §2.5。

### 5.4 背景と地色

汎用スライドの地色は `#F0F5FC` の不透明度 85%。
白背景では `#F3F7FC` になる。

半透明にしておくと、下に画像やイラストがあっても地色として成立する。
不透明度を変えれば1色で濃さを調整できる。
白背景に置いたときの値は次のとおり。

| 不透明度 | 白背景での値 |
|---|---|
| 100% | `#F0F5FC` |
| 85% | `#F3F7FC` |
| 60% | `#F7FAFD` |
| 40% | `#FAFCFE` |

地色はすべて白に近いので、文字は `#434343` のまま置ける ( `#F3F7FC` の上で比 9.20 )。

### 5.5 Googleスライドのテーマ配色

スライド → テーマを編集 → 色 で設定する。
グラフを挿入したときに自動で当たるのはアクセント1から順なので、並び順が実質のスイッチになる。

| 枠 ( Googleスライド ) | pptx の枠 | 入れる色 | トークン |
|---|---|---|---|
| テキストと背景 1 | テキスト1 ( dk1 ) | `#434343` | スライド本文色 |
| テキストと背景 2 | 背景1 ( lt1 ) | `#FFFFFF` | Surface |
| テキストと背景 3 | テキスト2 ( dk2 ) | `#5A74A3` | Primary Dark |
| テキストと背景 4 | 背景2 ( lt2 ) | `#F7F7F7` | Background |
| アクセント 1 | accent1 | `#5A74A3` | Primary Dark |
| アクセント 2 | accent2 | `#C4A55A` | Accent |
| アクセント 3 | accent3 | `#9BB0D0` | Primary Light |
| アクセント 4 | accent4 | `#84B5B2` | CAT-2 青緑 |
| アクセント 5 | accent5 | `#A4A5D1` | CAT-1 紫 |
| アクセント 6 | accent6 | `#E0E0E0` | Border |
| リンク | hlink | `#5A74A3` | Primary Dark |
| 表示済みリンク | folHlink | `#9BB0D0` | Primary Light |

新規のGoogleスライドは既定のテーマ配色 ( テキスト1 `#000000`、アクセント1 `#4285F4` ほか ) のままなので、全枠を上の表の値に入れ替える。 <!-- design-check: allow NO_TEXT_BLACK -->

#### 入れない色

- Point `#CD5151` … 自動で系列に当たると「ここを見て」の意味が壊れる。
  カスタム色として手で使う
- CAT-3 水色 `#98BBDE` ･ CAT-4 山吹 `#EDB369` ･ CAT-5 ローズ `#CB6378` … 案件ごとに変える層をテーマに焼き込むと「変えてよい色」が固定される。
  水色は Primary Light と ΔE 4.8 ･ L* 差 3.2 で、`FIGURE.md` §1.2 の代替条件 ( L* 12 以上かつ ΔE 12 以上 ) を満たさない

#### 入れ替えたあとの確認

1. 新しいスライドに 挿入 → グラフ → 縦棒 でスライド内蔵のグラフを置く。
   系列1が `#5A74A3`、2が `#C4A55A` になっていれば、スライド側のテーマ配色は設定できている
2. 既存スライドで手塗りしていた `#6E87B6` などは、テーマ色ではなくカスタム色として残るので変化しない <!-- design-check: allow NO_HARDCODED_PRIMARY -->
3. **この手順で確認できるのはスライド内蔵グラフだけ**。
   Googleスプレッドシートで作ってスライドにリンクとして貼ったグラフ ( リンク済みグラフ ) は、この設定を見ない。
   リンク済みグラフを使っている場合は §5.6 も合わせて設定し、§5.6 の確認手順まで行って初めて設定が終わったことになる

### 5.6 スプレッドシートのテーマ配色

Googleスプレッドシートは、スライドとは別に自分自身のテーマ配色を持つ。
グラフをスプレッドシート側で作り、スライドにリンクとして貼り付けた場合、そのグラフは貼り付け先のスライドのテーマ配色ではなく、
元のスプレッドシート自身のテーマ配色に従う。
§5.5 でスライド側を設定しても、リンク済みグラフの色は変わらない。
スプレッドシート側にも同じ配色を設定する必要がある。

表示形式 → テーマ → カスタマイズ で設定する。

| 枠 | 入れる色 | トークン |
|---|---|---|
| テキスト | `#434343` | スライド本文色と揃える |
| 背景 | `#FFFFFF` | Surface |
| グラフの色 1 | `#5A74A3` | Primary Dark |
| グラフの色 2 | `#C4A55A` | Accent |
| グラフの色 3 | `#9BB0D0` | Primary Light |
| グラフの色 4 | `#84B5B2` | CAT-2 青緑 |
| グラフの色 5 | `#A4A5D1` | CAT-1 紫 |
| グラフの色 6 | `#E0E0E0` | Border |
| ハイパーリンク | `#5A74A3` | Primary Dark |

並び順はスライドのアクセント1〜6と同じにする。
揃えておかないと、同じ資料の中でスライド作成のグラフとスプレッドシート由来のグラフで色が食い違う。

#### 入れ替えたあとの確認

1. スプレッドシート側の色を入れ替えたら、リンク先のスライドを開く
2. リンク済みグラフを選んで「更新」を押す
3. 系列1が `#5A74A3`、2が `#C4A55A` に変わっていれば設定できている。
   変わらないときはスプレッドシート側の色が反映されていないので、テーマのカスタマイズをやり直す

#### 運用

- **作成済みのグラフには遡及しない**。
  既存分はグラフ側で個別に色を入れ直す
- 分析担当に配るときは、配色済みの空スプレッドシートを1つ作って「コピーして使う」形にするのが確実
- 条件付き書式のカラースケールは既定が `#F3F3F3` → `#E67C73` の赤。
  `FIGURE.md` §2.2 の濃淡に差し替える

### 5.7 3カラムレイアウト

コンテンツ幅を3等分する。
値は実際のスライドから測ったもので、§5.2 のコンテンツエリアに収まる。

| 項目 | 値 |
|---|---|
| コンテンツ幅 | 9.21 in ( x 0.39 → 9.60 ) |
| カード幅 | 2.94 in |
| ギャップ | 0.20 in ( 目安 ) |
| カードの地 | `#F3F7FC` |
| カード内の見出し | Primary ･ 16pt ･ 太字 |
| カード内の本文 | `#434343` ･ 14pt ( 補足は 11pt ) |
| 枠線 ･ 影 | 影はなし。地色だけで面を作るのが基本形。白背景の角丸囲み ( `FIGURE.md` §1.3 ) も使えるが、Web UI 感が強くなるので推奨はしない <!-- design-check: allow NO_PERIOD_LINEBREAK --> |

2カラムならギャップ 0.24、4カラムなら 0.16 に狭める。

#### 型

| 場面 | やり方 |
|---|---|
| 3つが並列 | 地色カード ＋ 中央見出し。色は3つとも同じ <!-- design-check: allow NO_PERIOD_LINEBREAK --> |
| 3つに順序がある | 番号カードを付ける。色は変えない <!-- design-check: allow NO_PERIOD_LINEBREAK --> |
| 3つが別のカテゴリ | 見出し帯にカテゴリの文字色、地にカテゴリの地色 |
| 3つの数値を見せる | ドーナツを3つ。向き ･ 太さ ･ 色を揃え、値だけ変える <!-- design-check: allow NO_PERIOD_LINEBREAK --> |
| 1つが主役 | 残り2つは地を `#F7F7F7`、文字を `#666666` に落とす |
| 出典が要る | カード内に細い罫線を引いて下に置く |

#### やらないこと

- 3つのカードで高さを変える ( 内容量が違っても、地の高さは揃える )
- カードに枠線と影を両方付ける
- 並列の3つにカテゴリの3色を割り当てる ( 並列は同色。
  色分けは「別のもの」の合図 )
- ギャップを 0.20 in より広げて、カード幅を削る

### 5.8 装飾の線

角丸のカードの上や横に色帯を敷いて、色を付けただけの状態にしない。

| 使い方 | 扱い |
|---|---|
| テンプレートに元からある縦棒 ( タイトル左 `#7288B2` ) | 残す。版面の一部 <!-- design-check: allow NO_PERIOD_LINEBREAK --> |
| 見出しのデザインとして組み込まれた線 | 可 |
| 白背景の角丸囲み ( `#E0E0E0` ) | 可 ( `FIGURE.md` §1.3 ) |
| カードごとに反復する左3px ･ 上3px の色帯 | 使わない |
| 消しても意味が変わらない線 | 使わない |

階層は線ではなく、**面 ･ 余白 ･ 文字サイズ ･ 番号** で作る ( 作り方は `FIGURE.md` §3 )。
