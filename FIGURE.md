# FIGURE.md - 図表とスライドの規約

> 2026/9/8 時点の検討版。
> 値は実際のスライド ･ レポートから抽出して測ったもので、確定ではありません。
> 末尾に「未確定の観察項目」と「別フェーズに送るもの」を置いています。

---

## 0. このファイルの位置づけ

図表 ( グラフ ･ 図解 ･ 表 ) の描き方、見出しの階層、スライド固有の規約をまとめる。

| ファイル | 扱うもの | 読むとき |
|---|---|---|
| `DESIGN.md` | 色の値 ･ トークンの一次情報源、UIコンポーネント | 画面 ･ アプリを作るとき |
| `FIGURE.md` | 図表の描き方、見出しの階層、スライド固有の規約 | グラフ ･ 図解 ･ 表を作るとき、スライドを作るとき |

色の値の一次情報源は `DESIGN.md` とする。
このファイルは既存トークンを参照するだけで、値を複製しない。
独自の値を持つのは、`DESIGN.md` に対応がないものだけ。

`DESIGN.md` への変更は `## 2. Color Palette & Roles` の末尾に参照ブロックを足すだけとし、
`contracts/rules.json` と `index.html` には追加しない。
design-check は HTML を検査する仕組みなので、このファイルの規約のうちスライドに関するものは検査の対象外になる。

### 適用範囲

- スライド ( Googleスライド ･ PowerPoint )
- HTMLレポートの図版
- 図表を書き出した画像

§1 から §4 は媒体を問わず適用する。
§5 はスライドだけに適用する。

このファイルの ΔE はすべて **CIEDE2000**、コントラスト比は **WCAG 2.x** で計算している。
CIE76 とは値が大きく変わる色があるので、数値を引くときは計算式を確認する。

このファイルを上流とし、txtToHTML など下流のツールはここを参照する。
逆にツール固有の事情をこのファイルに持ち上げない。

### 近似色の検査 ( 2026/9/8 )

作例スライド44枚で実際に塗られている50色 ( 白 `#FFFFFF` を除く。
含めると51色 ) を総当たりで測り、CIEDE2000 で近い組を洗い出した。
ΔE 4 未満は 63 組あるが、そのほとんどは白に近い地色どうしで、同じ図の中に並ぶことがないため問題にしていない。

意図して近い値を残しているのは次の4組。
いずれも用途で分けている。

| 組 | ΔE | 扱い |
|---|---|---|
| `#D9D9D9` ( 表の罫線 ) と `#E0E0E0` ( Border ) | 1.6 | 線と面で役割を分ける ( §1.3 ) |
| `#CD5151` ( Point ) と `#D9534F` ( Danger ) | 3.0 | 注目の誘導と危険な操作。名前を分ける ( §1.4 ) <!-- design-check: allow NO_PERIOD_LINEBREAK --> |
| `#EDB369` ( 山吹 ) と `#F0AD4E` ( Warning ) | 3.6 | カテゴリと状態表示。同じ図に並べない ( 末尾の観察項目 ) <!-- design-check: allow NO_PERIOD_LINEBREAK --> |
| `#90BBE1` ( テンプレの章番号 ) と `#98BBDE` ( CAT-3 水色 ) | 1.8 | 章扉と本文ページなので同時に出ない ( 観察項目 ) |

段として並べる色は ΔE 4 以上離す ( §1.3 )。
この条件に触れる組は、上の4組を除いて出ていない。

---

## 1. 三層構造

色は1つのパレットではなく、性質の違う3層で構成する。
層ごとに「固定するもの」と「案件ごとに変えるもの」が異なる。

| 層 | 名称 | 扱い | 使う場所 |
|---|---|---|---|
| Layer 1 | ブランド基調 | 固定 ･ 全案件共通 | 見出し、帯、タイトル、単系列のグラフ |
| Layer 2 | カテゴリ配色 | 案件ごとに差し替え | 複数カテゴリの識別、図解のノード |
| Layer 3 | 強調 | 固定 ･ 意味が決まっている | 注目させたい1箇所 |

この区別を混ぜると、変えてよい色と変えてはいけない色の判断がつかなくなる。

グレーはこの3層のどれにも属さない。
本文 ･ 補足 ･ 非強調をつくる下地なので、§1.3 に分けて扱う。

### 1.1 Layer 1 : ブランド基調

既存トークンをそのまま使う。
図表のために新しい色を足さない。

| 名称 | 値 | 図表での用途 | 白文字の比 |
|---|---|---|---|
| Primary | `#6E87B6` | 表のヘッダ、帯、カードの見出し帯 | 3.62 <!-- design-check: allow NO_HARDCODED_PRIMARY --> |
| Primary Dark | `#5A74A3` | グラフの主色、pill、グラデーションの起点 | 4.71 |
| Primary Light | `#9BB0D0` | 濃淡の淡い側 | 2.21 |
| Accent | `#C4A55A` | 対比の2色目 | 2.37 |
| Accent Light | `#E0D5A8` | 対比 ･ 淡 | 1.44 |
| Accent Dark | `#B0923E` | アイブロウ、章番号、白文字を乗せる帯の終点 | 2.99 |
| SUB ( 深いゴールド ) | `#A18E6C` | 白文字を乗せるゴールドの面 ･ pill | 3.18 |
| Border | `#E0E0E0` | 非強調の系列 ･ ドーナツのトラック ( 面として使う ) | ― |
| Background | `#F7F7F7` | 注釈帯の地色、グラデーション時のトラック | ― |
| Text Secondary | `#666666` | 図解の比較対象 | 5.74 |
| Text Disabled | `#AAAAAA` | スライドでは使わない ( §1.3 ) | ― |

#### SUB ( 深いゴールド ) だけは独自の値

`#A18E6C` は `DESIGN.md` に対応するトークンがない。
Accent `#C4A55A` と ΔE 11.8、Accent Dark `#B0923E` と ΔE 10.7 で、どちらとも別の色。

ゴールド系で白文字を乗せたいときに、この色だけが条件を満たす。

| 色 | 白文字の比 |
|---|---|
| Accent `#C4A55A` | 2.37 |
| Accent Dark `#B0923E` | 2.99 |
| **SUB `#A18E6C`** | **3.18** |

Accent はグラフの系列 ( 対比の2色目 ) に使い、白文字は乗せない。
白文字を乗せるゴールドの面 ･ pill には SUB を使う。

#### Primary と Primary Dark の使い分け

面には Primary、グラフの主色には Primary Dark を使う。
一本化せず2値で持つ。
従来のスライドがすでにこの形で運用されていたため。

| 使う場所 | 色 | 根拠 |
|---|---|---|
| 見出し帯 ･ 表のヘッダ ･ カードの帯 | Primary `#6E87B6` | 従来の帯 `#7288B2` と ΔE 1.3 <!-- design-check: allow NO_HARDCODED_PRIMARY --> |
| 単系列グラフの主色 ･ 対比の1色目 | Primary Dark `#5A74A3` | 従来の KEY `#5C79A4` と ΔE 2.1 |
| pill ･ 番号カード ･ 図解の注目群 | Primary Dark `#5A74A3` | 四象限の pill `#5F75A1` と ΔE 1.3 |
| 濃淡の最も濃い段 ･ グラデーションの起点 | Primary Dark `#5A74A3` | Primary Light との差が ΔE 20.3 と広く取れる |

Primary Dark を主色にするのは、非強調の Border `#E0E0E0` との比が 3.57 で、
Primary の 2.74 より主役が立つため。
白文字も 4.71 で、本文サイズでも乗せられる ( Primary は 3.62 で太字に限る )。

2つは ΔE 7.3。
並べれば区別はつくが、別のスライドに分かれていると同じ青に見える。
1枚の中で「面は Primary、線と点は Primary Dark」と混在させてよい。

#### 図表での呼び名 ( エイリアス )

スライドを作る人が使う語彙。
値は既存トークンを指すだけで、複製しない。

| 呼び名 | 参照先 | 色差 |
|---|---|---|
| KEY | Primary Dark | ΔE 2.1 |
| KEY-light | Primary Light | ΔE 0.7 |
| SUB | 独自の値 `#A18E6C` | ― |
| SUB-light | Accent Light | ΔE 4.9 |
| SUB-dark | Accent Dark | ― |
| MUTED | Border | ΔE 4.1 |
| POINT | 独自の値 ( §1.4 ) | ― |

### 1.2 Layer 2 : カテゴリ配色

案件ごとに色そのものは変わってよい。
固定するのは値ではなく、選定条件のほうとする。

#### 選定条件

面の使い方で条件が分かれる。

**A. 淡い面 ( 文字を乗せない ･ 地と組む )**

- 明度 : L\* 68 〜 78 に収める
- 彩度 : 1セット内での C\* の差を 35 以内に収める

**B. 濃い面 ( 白文字を乗せる ･ pill や番号カード )**

- 白文字のコントラスト比 2.0 以上を確保する
- 大きめの太字で使い、本文サイズの白文字は乗せない

**共通**

- 色相 : 各色を 60° 以上離す。
  それが取れないときは、L\* を 12 以上かつ ΔE を 12 以上離す
- 明度で見分ける組は1図につき1組まで ( 2組以上入れると図全体が濃淡に見える )
- 色数 : 1案件あたり4色まで ( 定番は5色。
  そこから選ぶ )
- Layer 1 ･ Layer 3 の色と ΔE 15 以上離す

#### 定番セット

カテゴリ色は 面 ･ 地 ･ マーカー ･ 文字 の4値でひと組とする。

| 名称 | 面 ( fill ) | 地 ( 白90% ) | マーカー ( 面30% ) | 文字 |
|---|---|---|---|---|
| CAT-1 紫 | `#A4A5D1` | `#F6F6FA` | `#DDDEEE` | `#8D90CA` |
| CAT-2 青緑 | `#84B5B2` | `#F3F8F7` | `#D2E4E2` | `#5CA09D` |
| CAT-3 水色 | `#98BBDE` | `#F5F8FC` | `#D9E6F3` | `#659AC7` |
| CAT-4 山吹 | `#EDB369` | `#FDF7F0` | `#F8E3C8` | `#DE7C31` |
| CAT-5 ローズ | `#CB6378` | `#FAEFF2` | `#ECC5CD` | `#CB6378` |

Layer 1 の基調にも同じ4値を用意する。
カテゴリではないので Layer 2 には数えない。

| 名称 | 面 ( fill ) | 地 | マーカー ( 面30% ) | 文字 |
|---|---|---|---|---|
| 基調 ( Primary ) | `#6E87B6` | `#F3F7FC` | `#C9D3E4` | `#5A74A3` <!-- design-check: allow NO_HARDCODED_PRIMARY --> |

- 地は導出値 `#F0F3F8` が既存の `#F3F7FC` ( 汎用スライドの地色 ) と ΔE 0.9 なので、新しい値は作らず既存値を使う
- マーカー `#C9D3E4` の上に本文 `#434343` を置いたコントラスト比は 6.56
- 文字は Primary の比が 3.62 で本文サイズに届かないため、Primary Dark `#5A74A3` ( 比 4.71 ) を使う
- 紫のマーカー `#DDDEEE` と ΔE 5.0、水色のマーカー `#D9E6F3` と ΔE 4.9。
  同じ図の中で3つを並べない

- 面 … pill、見出し帯、番号カード、グラフの系列
- 地 … カードやタイルの背景
- マーカー … 文字の下半分に重ねる帯
- 文字 … 本文中の強調

CAT-5 ローズ `#CB6378` は L\* 55.0 で選定条件 A から外れるが、B として使われている。

#### 4値の導き方

面から次の手順で導出する。
面色がひとつ決まれば、残り3つは計算で出せる。

1. 地 : 面を白 90% で薄める
2. マーカー : 地の上に、面を不透明度 30% で置く
3. 文字 : 色相は面と同じ。
   ただし黄系のみ 15° 赤寄りにずらす
4. 文字 : 彩度は面の 1.35 倍
5. 文字 : 明度は白背景に対するコントラスト比が 3.0 に届くまで下げる

面のコントラスト比がすでに 3.0 以上ある色 ( CAT-5 ローズ 3.75 ) は、面と文字を分けない。
比 3.0 は大きめの太字の目安。
本文サイズの文字に使う場合は 4.5 まで下げる。

#### 白文字を乗せる面には「文字色」を敷く

pill ･ 見出し帯 ･ 番号カードのように白文字を乗せる面は、
面 ( fill ) ではなく **文字の色を面として使う**。

| 名称 | 面をそのまま敷く | 文字色を面として敷く |
|---|---|---|
| CAT-1 紫 | 2.36 | 3.01 |
| CAT-2 青緑 | 2.27 | 3.01 |
| CAT-3 水色 | 2.00 | 3.00 |
| CAT-4 山吹 | **1.87** | 2.99 |
| CAT-5 ローズ | 3.75 | 3.75 |

面をそのまま敷くと CAT-4 山吹が 1.87 で選定条件 B ( 2.0 以上 ) を割る。
文字色を敷けば5色とも 2.99 〜 3.75 に揃い、色ごとの見え方の差もなくなる。

淡い面を使うのは「文字を乗せない」場面 ( タイルの地、グラフの系列、マーカー ) に限る。

逆向きも同じで、白地に置く文字には面の色をそのまま使わない。
グラフの外に出す系列名や値のラベルは、面ではなく文字色を使う。
水色は面 `#98BBDE` が白地で比 2.00、文字色 `#659AC7` が 3.00。
面と文字は別の値だと考える。

#### 文字色を使ってよいサイズ

カテゴリの文字色は 4値の導出どおり、白背景に対する比が 3.0 前後になる。
WCAG で比 3.0 が許されるのは「大きめの文字」= 18pt ( 24px ) 以上、または 14pt ( 18.66px ) 以上の太字。

| 媒体 | 本文の太字 | 判定 | 必要な比 |
|---|---|---|---|
| スライド | 14pt 太字 | 大きめの文字に該当する | 3.0 |
| HTML | 16px 太字 | 18.66px 未満で該当しない | 4.5 |

スライドの本文 14pt は太字にすれば基準を満たす。
HTML の本文 16px を太字にしても基準に届かないので、次のどちらかにする。

- 強調する語だけ 18.66px 以上にする
- 文字は本文色のまま、マーカー ( 面を不透明度30% ) を敷いて示す

CAT-4 山吹の文字色 `#DE7C31` は比 2.99 で、3.0 をわずかに下回る。
1段だけ濃くした `#DE7B30` なら 3.01 で、`#DE7C31` とは ΔE 0.3。
見分けはつかない。

#### 既存トークンと近すぎる色に注意

基調に寄った青系どうしは色相条件を満たさない。
上の代替条件 ( L\* 12 以上かつ ΔE 12 以上 ) で判定すると、落ちるのは次の2組だけになる。

- 水色 `#98BBDE` と Primary Light `#9BB0D0` … L\* 差 3.2 ･ ΔE 4.8。
  同じ図に並べない
- 紫 `#A4A5D1` と 水色 `#98BBDE` … L\* 差 5.5 ･ ΔE 12.5。
  同じ図に並べない

基調と水色 ( L\* 差 25.9 ･ ΔE 22.8 )、基調と紫 ( 20.4 ･ 19.8 ) は代替条件を満たすので使える。
- 山吹 `#EDB369` は Warning `#F0AD4E` と **ΔE 3.6** で、ほぼ同じ色。
  カテゴリの識別に警告色と見分けのつかない色を使っている状態にあたる ( 末尾の観察項目 )

### 1.3 グレー

グレーは値を新しく作らない。
スライドで実際に選ばれている段から使う。

少しの明度差が見た目に強く出る色なので、パレットとして値を固定しすぎない。
図ごとにトーンが揺れることは許容する。

#### 使う段

スライド本体 ( レイアウト ･ マスターの既定値を除く ) で 50 回以上使われている5段。

| 値 | 本体での使用 | 白背景の比 | 用途 |
|---|---|---|---|
| `#434343` | 668 | 9.89 | 本文 |
| `#595959` | 131 | 7.00 | 補足 ･ 注記 |
| `#666666` | 138 | 5.74 | 図解の比較対象 |
| `#999999` | 178 | 2.85 | 図解の弱い群 |
| `#B7B7B7` | 59 | 2.01 | 非強調の面 ･ 罫線 |

50 回に届かないものは既定値に入れない。
`#212121` 43 / `#D9D9D9` 8 / `#EFEFEF` 3 / `#CCCCCC` 2 / `#4A4A4A` 1 / `#141414` 0。

#### 守ること

- **文字に使うグレー** … 本文サイズなら比 4.5 以上 ( `#767676` まで )、大きめの太字なら 3.0 以上 ( `#888888` まで )
- **面 ･ 罫線 ･ 非強調の系列** … 下限は「白と見分けがつくこと」= 比 1.2 以上。
  値は図ごとに変えてよい
- **段として並べるとき** … 隣り合う段を ΔE 4 以上離す。
  それ以下だと段ではなく揺れに見える
- **例外** … 図解の弱い群のように読ませない意図の文字は、比 3.0 を割ってよい。
  1つの図に1役割まで

`#999999` ( 2.85 ) と `#B7B7B7` ( 2.01 ) は文字の基準を割っているが、
どちらも「読ませない」ことが目的の役割なので、上の例外に当たる。

`#595959` と `#666666` は ΔE 4.8 と近い。
同じ図の中では両方を段として使わない。

#### 線の引き方

スライドで実際に引かれている線を数えると、圧倒的に「引かない」。

| 線の指定 | スライド本体での件数 |
|---|---|
| **枠線なし ( noFill )** | **1,241** |
| `#595959` | 34 |
| `#999999` | 15 |
| `#B7B7B7` | 8 |
| `#666666` | 7 |

線は表の枠線だけにする。
図形には引かず、面と余白で形をつくる。

線を引くのは、区切り ･ 引き出し ･ 補助のように**線そのものに意味があるとき**だけ。
そのときは `#999999`、強めに見せたいときは `#595959` を使う。

| 用途 | 値 |
|---|---|
| 図形の枠線 | 引かない |
| **白背景の角丸囲み** | **`#E0E0E0` ･ 0.75pt ･ 角丸 0.08in ( 可 )** |
| 区切り線 ･ 引き出し線 | `#999999` |
| 強めの区切り | `#595959` |
| 表の罫線 ･ グラフの目盛り線 | `#D9D9D9` |
| 表の外枠 | `#D9D9D9`。表は外側にも線を引く <!-- design-check: allow NO_PERIOD_LINEBREAK --> |
| 見出しの下の細罫 | `#E0E0E0` ･ 0.75pt ( §3 ) |

#### 白背景の角丸囲みは可

地色を敷かずに白のまま見せたいとき ( 色見本 ･ カラーパレットなど、地色が色の見え方に影響する場面 ) は、
白背景に `#E0E0E0` の薄いボーダーを付けた角丸の囲みを使ってよい。
Web でも同じ形を使っているため、媒体をまたいで同じ見え方になる。

- 枠線 `#E0E0E0` ･ 0.75pt ･ 角丸 0.08in ･ 内側マージン 0.5cm
- 地色を敷くカード ( `#F3F7FC` など ) と併用しない。
  1つの資料の中ではどちらかに統一する
- 「とりあえず囲む」用途には使わない。
  白のままにしたい理由があるときだけ
- 角丸そのものは可。
  ただし角を丸めるほど Web UI に見えるので、KPIカードのような面の大きい要素では積極的には使わない

表の罫線だけはテンプレートに表が1つも入っていないため、実際のスライドの画像から測った値。
HTML の Border `#E0E0E0` とは ΔE 1.6 で見分けはつかないが、値としては分けておく。

### 1.4 Layer 3 : 強調

| 名称 | 値 | 用途 |
|---|---|---|
| Point | `#CD5151` | 「ここを見て」の意味のみ |

案件で変わらない唯一の色。
カテゴリの識別には使わない。
1スライドにつき1箇所を原則とする。
面ではなく文字色として使うほうが、面積が小さく済み意味が立つ。

Point `#CD5151` は Danger `#D9534F` と ΔE 3.0 で見た目はほぼ同じだが、意味が違う。
Danger はエラー ･ 削除 ･ 危険な操作、Point は注目の誘導。
名前は必ず分けて使う。

### 1.5 色に意味を持たせるときの判断

色そのものではなく、色のどの軸が何を担うかを決める。

| 軸 | 担うもの |
|---|---|
| 濃さ | 量 ･ 強弱 ･ 順序 |
| 色相 | 順序のないカテゴリの識別 |
| 無彩色 | 値ではないもの ( 比較対象 ･ 非該当 ･ 見出し ) |

そのうえで、次の順に見る。

1. データにカテゴリがあり、その意味が資料全体で一貫するときだけ色相を分ける
2. 順序 ･ 量 ･ 強弱は色相ではなく濃さで示す
3. 慣習の色 ( 赤 = 危険、緑 = 良い ) は、同じ資料に警告や正負の表現が無いときだけ使う
4. 1つの資料の中で、同じ色を2つの意味に使わない

実務でいちばん効くのは 4 。
ヒートマップの塗りを表ごとに変えないのも、2群の色を資料内でそろえるのも、理由はここにある。

---

## 2. グラフの描き方

### 2.1 色数

1つのグラフで使う色は原則2色。
3色目が必要かどうかを先に疑う。

| 見せたいこと | 使う色 |
|---|---|
| 片側だけを読ませたい | 主役 Primary Dark ＋ 残り Border |
| 両端に意味がある対比 ( 昼と夜、自社と競合 ) | Primary Dark ＋ Accent |
| 順序のあるデータ ( 高 ･ 中 ･ 低 ) | 同系の濃淡 |
| 順序のないカテゴリ | Layer 2 を4色まで |
| 注目させたい1点 | Point を1箇所 |

「正負を分ける」という括りは使わない。
符号ではなく、両端に意味があるかどうかで決める。

強調のつけ方は §2.12 にまとめている。

性別 ･ 年代のようなセグメントも「順序のないカテゴリ」に当たる。
男性を青 ･ 女性を赤のように分けると、赤が Point ･ Danger と衝突して
片方が警告に見えるので、Layer 2 から選ぶ。

### 2.2 濃淡の段階

順序尺度は色相を変えず、明度だけを段階的に変える。

左端が濃く、右へ行くほど淡い。
凡例を読まなくても順序が分かる状態にする。

段の値は媒体で変えてよい。

**スライド ( 標準 )**

| 段 | 値 | 白文字の比 |
|---|---|---|
| 1 | `#33559A` | 7.22 |
| 2 | `#6280BC` | 3.94 |
| 3 | `#9BACD2` | 2.28 |
| 4 | `#D6DCEC` | 1.37 |

隣り合う段の ΔE は 16.0 / 15.2 / 14.4。
白文字が乗るのは2段目まで。
3段目からは値を `#434343` にする。

投影すると淡い段が飛ぶので、起点を基調より濃くしている。
基調 Primary Dark から始めると2段目の白文字比が 2.96 になり、スライドの基準 3.0 を割る。

**Web ･ HTMLレポート**

| 段 | 値 | 白文字の比 |
|---|---|---|
| 1 | `#5A74A3` | 4.71 |
| 2 | `#8496BE` | 2.96 |
| 3 | `#AEBBD6` | 2.09 |
| 4 | `#D8DEEA` | 1.36 |

基調 Primary Dark から始める従来の4段。
隣り合う段の ΔE は 12.9 / 13.2 / 14.6。
画面は投影より淡い段が残るので、この起点でよい。
スライド用の4段を使ってもよい。

どちらの4段も、本文サイズの白文字は乗らない。
値は `#434343` にするか、棒の外に出す。

表のカラースケール ( 値が高いほど濃い ) も順序尺度なので、この濃淡を使う。
赤系のスケールは使わない。
Point ･ Danger と意味が衝突する。

### 2.3 軸の起点

軸を 0 から始める必要があるのは、**長さや面積で量を読ませる図** だけ。

| 図の種類 | 目で読んでいるもの | 0 起点 | 理由 |
|---|---|---|---|
| 棒 ･ 面 ･ 積み上げ | 原点からの長さ ･ 面積 | 必要 | 切ると長さの比が壊れる |
| ダンベル ･ 前後比較 | 2点の位置と、その間隔 | 不要 | 原点が符号に含まれていない |
| ドットプロット | 点の位置 | 不要 | 同上 |
| 折れ線 | 傾きと上下 | 不要 | ただし切った旨を軸に書く |
| 散布図 ･ 対応分析 | 点どうしの距離 | 不要 | 縦横を同じ縮尺にするほうが効く |

#### 切るときの条件

- 軸の目盛りを必ず出す。
  範囲を隠さない
- 同じページで並べる図は、同じ範囲に揃える
- 範囲はデータの最小 ･ 最大から少し広げた切りのいい値にする

値が狭い範囲に集まっているのに 0 起点のままにすると、
点が端に寄って差が読めなくなる。
実測では、値が 60 〜 84% に集まったダンベル図で、
点が使える幅 1,383px のうち 394px ( 28% ) にしか乗っていなかった。
軸を 55 〜 90 にすると 1pt あたり 39.5px になり、
最大 13.6pt と最小 5.6pt が別の長さとして読める。

#### 突出した値

棒グラフで1本だけ突出しているときも、軸は切らない。
突出している事実そのものが結論なので、その1本だけ Point にして、注釈で数字を説明する。
目盛りを省略した軸、3D、影は使わない。

### 2.4 2群を並べる

地色で群を分け、目盛りの最大値は必ず揃える。
揃えないと高さの比較が意味を失う。

### 2.5 図解 ( 2軸4象限など )

点ではなく名前を置くと、凡例が要らない。
色は個体ではなく群につける。

| 役割 | 値 |
|---|---|
| 注目する群 | Primary Dark |
| 比較対象 | Text Secondary `#666666` |
| 弱い群 | `#999999` ( §1.3 ) |
| 領域のラベル | 大きな薄い円 `#E6EBF7` に置く。円は正円にする <!-- design-check: allow NO_PERIOD_LINEBREAK --> |

領域の円は `#E6EBF7`。
白との比は 1.19 で、面の下限 1.2 をわずかに割っている。
群を囲む下地なので輪郭を読ませる必要はないが、下限ぎりぎりであることは覚えておく ( 末尾の観察項目 )。

領域の面の色で意味を持たせる。
象限の地は Background より薄い `#F8FAFD` 程度にとどめる。

#### 色分けする場合

象限ごとに地色を敷き、見出しにマーカーを引く。
キーワードは線囲みのラベルにする。

### 2.6 表

| 部位 | 扱い |
|---|---|
| ヘッダ行 ･ 見出し列 | Primary で塗り、白の太字 |
| 罫線 ( 横 ･ 縦 ･ 外枠 ) | `#D9D9D9` ( §1.3 )。**外側にも線を引く** <!-- design-check: allow NO_PERIOD_LINEBREAK --> |
| セルの地 | 白 |
| サブの見出しや強調する行 | 太字にするか、薄い青 `#EBEEF4` で塗る |

別の色を使う場合は CAT-2 青緑 か CAT-1 紫 を優先する。
見出しだけ色を替え、罫線とセルの地は変えない。

基本形は **縦罫と外枠のある形**。
行数が多いときや、資料の中で表を目立たせたくないときは、
罫線を使わず地色の縞 ( `#F7F7F7` ) で分ける形もとれる。
これは例外とする。
基本形と混在させない。
1つの資料の中ではどちらかに統一する。

ヘッダに Excel ･ Googleスプレッドシートの既定の濃紺 `#305496` が入っていることがある。
Primary Dark とは ΔE 11.7 の別色なので、Primary に置き換える。

### 2.7 グラデーション

色で値を比べない場所には、積極的に使ってよい。

| 記号 | 名称 | 値 | 中間色 | 用途 |
|---|---|---|---|---|
| A | 標準 ･ 同系の濃淡 | Primary Dark → Primary Light | `#7A92BA` | 単系列のバー、エリアの下地、ドーナツ |
| B | 装飾 ･ 青 → 青緑 | Primary Dark → `#84B5B2` | `#6F94AA` | KPIカード、ドーナツ、見出し帯 |
| D1 | 対比 ･ 淡 | Primary Dark → Accent Light | `#9DA4A5` | 図の地色、対比の軸 |
| D2 | 対比 ･ 濃 | Primary Dark → Accent Dark | `#858370` | 白文字を乗せる帯、章扉 |

D1 ･ D2 は青と暖色で色相が反対側にあるため、中央がグレーに寄る。
帯として使うぶんには明度が揃うので、文字が安定する。
D1 で白文字を乗せられるのは左から 35% まで ( 目安 )。
35% の地点 `#8996A5` でコントラスト比 3.01、50% の地点 `#9DA4A5` で 2.53 まで落ちる。

ドーナツやKPIカードに使うときは、すべて同じ角度にそろえる。
角度がばらつくと、面ごとに別の意味があるように見える。

CSS では `linear-gradient( in oklab, ... )` を使うと中間の濁りが軽減される。
Googleスライドは中間点を手で追加できるので、2色で濁る場合は上表の中間色を挟む。

#### 使わない場所

- 複数系列の比較バー : 同じ値でも位置によって明るさが変わり、長さの比較が崩れる
- カテゴリの識別色 : グラデーション同士は見分けがつかない
- 強調色 ( Point ) : 「ここを見て」の意味が薄まる
- D1 ･ D2 をグラフの系列に : 中央付近の値だけ色が抜けて見える
- **D2 を円 ･ ドーナツに** : 中間の `#858370` は彩度 C\* 10.9 まで落ち、弧の途中でリングが一度死ぬ

### 2.8 円グラフ ･ ドーナツ

円は塗り分けずドーナツにする。
1値でも2 〜 3区分でも同じで、区分の数に応じた作りは §2.8.4 と §2.8.5 に従う。

#### 2.8.1 標準形

| 項目 | 値 |
|---|---|
| 色 | グラデーション A ( `#9BB0D0` → `#5A74A3` ) または B ( `#84B5B2` → `#5A74A3` ) |
| 向き | 弧に沿わせる ( 円錐 ) ･ 淡 → 濃 ･ 12時から時計回り |
| トラック ( 残り ) | `#F7F7F7` Background |
| リングの太さ | 直径の 0.22 ( 範囲 0.18 〜 0.28 ) |
| 内円の直径 | 直径の 0.56 ( 標準時 ) |
| 中央の数値 | 内円径の **28%** ･ 太字 ･ `#434343` |
| 中央のラベル | 数値の 50% ･ 最小 10pt ･ `#666666` ･ 数値の下 |
| 端の処理 | 直角。角丸にすると値が実際より大きく見える <!-- design-check: allow NO_PERIOD_LINEBREAK --> |

中央に置く文字は、横幅を内円径の 80% 以内に収める。
桁数やラベルが長くて収まらないときは、収まる大きさまで下げる。
下限は内円径の 18%。
それより小さくなるときは中央に置かず、外に出す。

実測では `20%` の3文字が 28% で幅 50%、`n=1,200` の7文字は 21% まで下げて 80% に収まる。

#### 2.8.2 トラックの色

グラデーションを掛けると、リングの淡い端がトラックに溶ける。

| リングの端の色 | トラック `#E0E0E0` | トラック `#F7F7F7` |
|---|---|---|
| `#5A74A3` Primary Dark | 3.57 | 4.39 |
| `#9BB0D0` Primary Light ( A の終点 ) | **1.67** | 2.06 |
| `#84B5B2` 青緑 ( B の終点 ) | **1.72** | 2.12 |

非文字要素の見分けの目安は比 3.0。
2.0 を下回ると輪郭として成立しない。
対策は2つあり、どちらでもよい。
併用してもよい。

1. **向きを反転する** : 淡から始めて濃で終わる。
   値の終点の比が 4.39 になる
2. **トラックを薄くする** : `#E0E0E0` を `#F7F7F7` に落とす。
   比が 2.06 に戻る

フラット塗り ( グラデーションなし ) のときは、トラックは `#E0E0E0` のままでよい。

#### 2.8.3 太さ

| 比 | 扱い |
|---|---|
| 0.14 | 細すぎる。線に見えて主役にならない <!-- design-check: allow NO_PERIOD_LINEBREAK --> |
| 0.18 | 下限 ･ 可。数値を大きく見せたいとき <!-- design-check: allow NO_PERIOD_LINEBREAK --> |
| **0.22** | **標準** |
| 0.28 | 上限 ･ 可。リングを主役にしたいとき <!-- design-check: allow NO_PERIOD_LINEBREAK --> |
| 0.32 | 太すぎる。内訳を期待させる <!-- design-check: allow NO_PERIOD_LINEBREAK --> |

太さを変えたときは、中央の数値も一緒に縮める。
内円から数値がはみ出すと、リングが数値の背景に見える。

中央の数値は内円径の 28%。
実測でいちばん収まりがよかった比で、内円径 1.20in に対して約 24pt にあたる。
これより大きくすると内円の余白が詰まり、リングが数値の背景に見えはじめる。
数値とラベルの間は 8pt 以上あける。

#### 2.8.4 分割数

| 区分 | 扱い |
|---|---|
| 1値 ＋ 残り | グラデーション可。残りは値ではなく地なので、色で比べていない <!-- design-check: allow NO_PERIOD_LINEBREAK --> |
| 2 〜 3 区分 | フラットのみ。§2.8.5 の作りにする <!-- design-check: allow NO_PERIOD_LINEBREAK --> |
| 4 区分以上 | 円をやめて横棒にする。面積の比較が目で効かない <!-- design-check: allow NO_PERIOD_LINEBREAK --> |

円グラフとして塗り分けた形は使わない。
区分が2つでも3つでもドーナツにする。

#### 2.8.5 2 〜 3区分のドーナツ

| 項目 | 値 |
|---|---|
| リングの太さ | 直径の 0.22 |
| 値の置き場所 | リングの外 ･ 系列名の下に % |
| 中央 | 主役の値 ＋ ラベル |
| 中央が収まらないとき | 空にして、外側に凡例やラベルを持たせる |

中央に値を出した区分は、外側から % を落として1箇所だけにする。
中央の文字の大きさは §2.8.1 と同じ扱いにする。

配色は2通りある。

| 種類 | 値 | 使う場面 |
|---|---|---|
| 単色系 | 濃淡3段 | 区分に順序があるとき |
| 多色系 | `#5A74A3` ･ `#5CA09D` ･ `#98BBDE` | 区分が順序のないカテゴリのとき |

多色系は基調 ･ CAT-2 青緑の文字色 ･ CAT-3 水色の3色。
青と水色は色相差 16.5° だが L\* 差 25.9 ･ ΔE 22.8 で、§1.2 の代替条件を満たす。
3区分のドーナツは3つとも互いに隣り合うので、並び順で色の近い2色を離すことはできない。

外に出すラベルの文字は面の色をそのまま使わない。
水色は面 `#98BBDE` ではなく文字色 `#659AC7` を使う。

#### 2.8.6 複数のドーナツを並べる

向き ･ 太さ ･ 色をすべて揃え、値だけが違う状態にする。
角度や色を変えると、別の意味があるように見える。

### 2.9 コレスポンデンス分析 ･ 散布図

#### 2.9.1 描き方

| 項目 | 値 |
|---|---|
| ブランド ･ 主体の点 | Primary Dark `#5A74A3` ･ 丸 |
| 項目 ･ 属性の点 | Accent `#C4A55A` ･ 菱形 |
| 主体のラベル | `#5A74A3` ･ 太字 |
| 項目のラベル | `#666666` ･ 標準 |
| 注目させる1つ | Point `#CD5151` ･ 1枚に1つまで |
| 原点の線 | `#D9D9D9` ･ 1px ( §1.3 ) |
| 目盛りの grid | 引かない |
| 四辺の枠 | 引かない |
| 凡例 | 枠なし ･ 横並び |

種類が違うものは色だけでなく形も変える。
既定パレット ( matplotlib の `#1F77B4` `#FF7F0E` など ) をそのまま使わない。

ラベルは、右 → 左 → 上 → 下 の順に空いている側を探して逃がす。
重なりは機械で消し、手で動かさない。

#### 2.9.2 縦横比

比率はブラウザ幅やスライドサイズで変えない。
**必ず固定する**。
開くたびに比率が変わると、同じ図が違う印象になる。

そのうえで、目的によって形を選ぶ。

| 目的 | 形 |
|---|---|
| 点どうしの距離を読ませる | 縦横 1 : 1。カード1枚を横いっぱいに使う <!-- design-check: allow NO_PERIOD_LINEBREAK --> |
| どちら側に寄っているかを見せる | 第1軸だけの1次元にする |
| 見慣れた散布図で出す | 縦を伸ばしてよい。ただし軸ラベルに「縦の差は読まない」と書く <!-- design-check: allow NO_PERIOD_LINEBREAK --> |

第1軸の寄与率が 90% を超えているときは、その分析は実質1次元。
1次元に落とすほうが、順序と距離がそのまま読める。
円グラフを横棒に置き換えるのと同じ判断。

縦横比を伸ばすと、寄与率の低い軸の差が実際より重要に見える。
実測では、寄与率 92.5% : 5.6% ( 16.5 : 1 ) の図で縦軸が 2.41 倍に引き伸ばされていた。

### 2.10 軸 ･ 目盛り ･ 値ラベル ( 2026/9/8 追加 ･ 検討版 )

数字と指標が読み手の判断材料になる。
装飾は減らしてよいが、軸 ･ 目盛り ･ 値は減らさない。
「何の線か」が軸だけを見て分かる状態を最低条件とする。

| 要素 | 扱い | 値 |
|---|---|---|
| 軸の見出し ＝ 何の値か ＋ 単位 | **必ず付ける** | 9 〜 10pt ･ `#666666` ･ 横軸は右寄せ、縦軸は軸の上 |
| 目盛りラベル | **必ず出す**。切った軸は始点を見出しにも書く ( §2.3 ) | 9 〜 10pt ･ `#666666` <!-- design-check: allow NO_PERIOD_LINEBREAK --> |
| 値ラベル | **全部の棒 ･ 主役の線に付ける**。入らないときは省いた旨を注記する | 9 〜 10pt ･ 本文色。主役は太字でよい <!-- design-check: allow NO_PERIOD_LINEBREAK --> |
| 基線 | 軸の位置を示す線として残す | `#D9D9D9` ･ 0.75pt ( §1.3 ) |
| 目盛り線 | 要るときだけ ( 折れ線 ･ 縦棒で高さを読ませるとき ) | `#D9D9D9` ･ 0.75pt |
| 凡例 | 2系列以上で付ける | 枠なし ･ 横並び ･ 図の上か線の脇 |
| 図形の枠線 ･ 四辺の枠 | 引かない ( §1.3 ) | ― |

目盛りラベルとグラフの軸ラベルはサイズの例外 ( §5.1 ) にあたるので、10pt 以下まで下げてよい。
桁を揃える必要がない軸ラベルに等幅フォントは使わない。

#### 例外

- 位置で読ませる図解 ( 2軸4象限 ･ 対応分析 ) は値ラベルを省いてよい。
  省いても軸の見出しで「何の軸か」は示す
- 100% 積み上げで幅が 5% に満たないセグメントは値を省いてよい。
  省いたら注記する
- 比較対象の線 ( Border に落とした系列 ) は終点の値だけでよい

#### 観察

v6 作例の初版は値ラベルだけの直接表記で、軸線 ･ 目盛り ･ 軸の見出しを描いていなかった。
軸を切ったダンベル図に目盛りがなく、§2.3 の「切るときは目盛りを必ず出す」と矛盾していた。
この節は「理想の見た目」として先に置いたもので、実運用のスライドで軸 ･ 見出しがどの程度付いているかは未集計。

### 2.11 ヒートマップ ( クロス集計表 )

数値はセルに入れ、色は偏りの形を見せる役に回す。

#### 2.11.1 正規化

| 単位 | 読ませるもの |
|---|---|
| 表全体 ( 標準 ) | セルどうしの大小 |
| 行ごと | 行の中の順位 |

表全体は表のいちばん大きい値を最濃にする。
行ごとにすると行の中の順位は読めるが、行をまたいだ濃さの比較はできなくなる。
列ごとは使わない。
全体が小さい列でも最濃が出るので、小さい値が大きく見える。

#### 2.11.2 段数

| 数値の扱い | 段数 |
|---|---|
| セルに数値を入れる ( 標準 ) | 7段 |
| 色だけで見せる | 5段 |

数値が入っていれば正確な値は数字が担うので、段は偏りの粒度として細かくしてよい。
色だけで見せるときは凡例が唯一の手がかりになるため、段をはっきりさせる。
隣り合う段の ΔE は 7段で 6.0 〜 7.2、5段で 9.0 〜 10.6。
どちらも刻みの基準 4 を満たす。

#### 2.11.3 色

塗りは青を標準とし、上端は `#6779B1` で止める。
見出し行と同じ色相なので、最濃まで使うと見出しが階調の一部に見える。
セルの文字は上2段だけ白にし、それ以外は `#434343` にする。
例外としてローズ ( CAT-5 ) を用意する。
赤系が求められる資料と、同じ資料に青の系列グラフが多く出る場合に使う。
1つの資料の中では1色に統一する。
表ごとに色を変えると、色そのものが意味を持っているように読まれる ( §1.5 の 4 )。

#### 2.11.4 見出し行

列見出しの帯は Primary Dark のままにし、行見出しの列は白地にする。
面を敷くのは「値を表すセル」と「列見出し」の2種類までにとどめる。

#### 2.11.5 発散配色

基準となる値があり、上下の向きに意味があるときだけ使う。
全体を100としたインデックスのような指標が該当する。

| 位置 | 色 |
|---|---|
| 下回る側 | Accent Dark `#B0923E` |
| 中央 | `#F5F4F1` |
| 上回る側 | `#6779B1` |

中央を白にしない。
値のないセルと基準ちょうどのセルが同じ見え方になる。
ただの大小には使わない。

### 2.12 強調と非強調

読ませたいものだけ色を残し、残りは無彩色に寄せる。
色を足して強調をつくらない。
この考え方は図の種類を問わない。

#### 2.12.1 残す範囲

| 残す範囲 | 読ませるもの |
|---|---|
| 全部 | 並びの形そのもの |
| 上位の段だけ | その段までの合計 |
| 1段だけ | いちばん強い1段 |
| 1つの項目だけ | 段ではなく項目 |

積み上げ棒なら段、横棒や折れ線なら項目や系列にあたる。

#### 2.12.2 無彩色の段

残りに順序があるときだけ段に分ける。
横棒や散布図のように残りが並列なら1色でよい。
ドーナツや積み上げ棒のように順序が残るなら段にする。

| 段数 | 値 | 隣り合う ΔE |
|---|---|---|
| 3段まで | `#C4C4C4` `#D8D8D8` `#ECECEC` | 4.8 / 4.4 |
| 4段 | `#B7B7B7` `#C9C9C9` `#DBDBDB` `#EDEDED` | 4.6 / 4.3 / 4.0 |

3段までなら4段より薄く始められる。
4段にすると `#C4C4C4` 起点では ΔE 3.2 〜 3.7 まで落ちて基準を割る。
値は目安で、図ごとに変えてよい ( §1.3 の「面 ･ 罫線 ･ 非強調の系列」)。
段として並べるときだけ、隣り合う段を ΔE 4 以上離す。

起点が濃すぎると、残りが「弱めた主役」ではなく「もうひとつの主役」に見える。
`#999999` から始めると、残りの段のうち上2段が主役級の重さになる。

#### 2.12.3 色以外も一段変える

面 ( 棒 ･ セル ･ 扇形 ) は色だけでよい。
線と点は面積が小さいので、太さと大きさも一段変える。
色だけだと、無彩色にした線でも主役の線と見分けにくい。

---

## 3. 見出しの階層

階層は線ではなく、**面 ･ 余白 ･ 文字サイズ ･ 番号** で作る。
色帯を1本足しても段は増えない。

### 3.1 作り方

上ほど強い。
ただし目安なので、どの段にどれを当てるかは資料ごとに決めてよい。

| # | 作り方 | 値 |
|---|---|---|
| 1 | 左に番号を置き、地を薄青で塗る | 地 `#F3F7FC` ( §5.4 ) |
| 2 | 見出しの下に全幅の細罫を引く | `#E0E0E0` ･ 0.75pt ( §1.3 ) |
| 3 | 青字 ＋ マーカー ＋ 太字 | 文字 `#5A74A3` ･ マーカー `#C9D3E4` ( §1.2 ) |
| 4 | 青字 ＋ 太字 | `#5A74A3` |

`#5A74A3` は Primary Dark。
白背景でのコントラスト比 4.71 で、本文サイズでも読める。
Primary `#6E87B6` は 3.62 で本文サイズに届かないため、見出しの文字色には使わない。 <!-- design-check: allow NO_HARDCODED_PRIMARY -->

近くでマーカーを使っていて見出しと区別がつかないときは、
本文色の太字 ( スライド `#434343` ･ HTML `#333333` ) に落としてよい。

### 3.2 左の色帯

見出しの左に色帯を敷くのは、スライドテンプレートに元からある縦棒 ( タイトル左 `#7288B2` ) だけとする。
これは版面の一部で、装飾ではない。

- カードや項目ごとに反復する左3px ･ 上3px の色帯は使わない
- 消しても意味が変わらない線は引かない

Web でも、スライドの見出しと見せ方をそろえたい場合に限り同じ形を使ってよい。
HTMLレポートの見出し設計そのものは txtToHTML の `specs/report-html-spec-v2.md` を一次情報源とする。

---

## 4. Do's and Don'ts

### Do

- 1つのグラフで使う色は原則2色。
  3色目が必要かどうかを先に疑う
- 順序のあるデータは色を変えず、同系の濃淡で表す
- 強調しない系列は Border に落とす
- 片側だけを読ませたいときは、主役を Primary Dark、残りを Border にする
- 両端に意味がある対比は Primary Dark と Accent の2色にする
- 白文字を乗せる面には、面色ではなくカテゴリの文字色を敷く
- 外部から受け取ったグラフ画像は、貼らずに描き直す
- 軸の見出し ( 何の値か ＋ 単位 ) ･ 目盛り ･ 値ラベルを付ける。
  何の線かが軸だけで分かる状態にする

### Don't

- 系列ごとに異なる色相を割り当てない ( 信号色の3色は特に避ける )
- 順序尺度に3色のカテゴリ配色を使わない
- 順序尺度のカラースケールに赤系を使わない
- Point `#CD5151` をカテゴリの識別に使わない
- Warning `#F0AD4E` ･ Danger `#D9534F` ･ Success `#5CB85C` をカテゴリの識別に使わない
- 面の色をそのまま文字色に使わない
- 突出値のために軸を途中で切らない
- 軸の見出しも単位もないグラフを出さない。
  目盛りのない切った軸を出さない
- 既定パレットのまま出さない ( Google `#4285F4` `#EA4335` `#FBBC04` / matplotlib `#1F77B4` `#FF7F0E` / スプレッドシートのカラースケール `#E67C73` )

---

## 5. スライド固有の規約

ここから先はスライドだけに適用する。
HTMLレポートには `DESIGN.md` のタイポグラフィを使う。

### 5.1 タイポグラフィ

| 項目 | 値 |
|---|---|
| 本文の色 | `#434343` |
| 本文のサイズ | 14pt |

#### フォント

| 用途 | フォント名 | 備考 |
|---|---|---|
| 和文 ･ 本文 | `Zen Kaku Gothic New Medium` | 中字。フォント名にそのまま Medium を含める <!-- design-check: allow NO_PERIOD_LINEBREAK --> |
| 欧文のみの語 | `Noto Sans JP` | 日付、Komorebi inc.、色名など |
| 等幅 | `Roboto Mono` | カラーコード、トークン名、桁を揃えて読む数値 |

pptx でウェイトを指定する方法はないので、中字はフォント名を `Zen Kaku Gothic New Medium` と書く。
`Zen Kaku Gothic New` だけだと Regular になる。

等幅は「桁を縦に揃えて比較する場所」と「コードとして読ませる場所」に限る。
グラフの軸ラベルなど、桁を揃える必要がない数値には使わない。

HTML側の等幅は `DESIGN.md` の `SFMono-Regular, Consolas, Menlo` のまま。
Googleスライドにこれらのフォントがないため、スライドだけ `Roboto Mono` を使う。

`#434343` はスライドで長く使われてきた値で、Text Primary `#333333` と ΔE 5.2。
白背景でのコントラスト比は 9.89 と 12.63 で、どちらも本文として十分。
統一する理由が特にないため、スライド側は `#434343` のままとする。

#### サイズの段階

`10 / 12 / 14 / 16 / 18 / 20 / 24 pt`

基準は 14pt。
これより小さいサイズは、レイアウト上どうしても入らない場合の例外に限る。
入らないときは文字を小さくするのではなく、要素を減らすか分割する。

グラフの凡例と軸ラベルは例外とし、10pt 以下まで下げてよい。

#### 目安

| 要素 | サイズ |
|---|---|
| スライドタイトル | 18pt |
| ブロックの見出し | 16 〜 20pt ･ 太字 ･ Primary Dark |
| 本文 | 14pt |
| サブラベル ･ hex ･ 数値 | 12pt |
| 下部の注記 | 11pt |

### 5.2 配置

スライドサイズは 10 × 5.625 in ( 16:9 )。

| 要素 | 位置 |
|---|---|
| 小見出し | y 0.40 |
| タイトル | y 0.65 |
| コンテンツエリア | x 0.39 〜 9.60、y 1.20 〜 5.22 |
| 出典 ･ 注記 | y 5.37 |

小見出しとタイトルは、テンプレートのレイアウトに用意されているプレースホルダーに入れる。
新しいテキストボックスを重ねると、空のプレースホルダーが下に残って見出しが二重に見える。
プレースホルダーは x 0.60 から始まる。
コンテンツは x 0.39 から始めるので、見出しとコンテンツで左端は揃わない。
これはテンプレートの仕様。

### 5.3 pill ･ マーカー ･ カード

カテゴリ配色の4値をそのまま使う。

| 素材 | 使う値 |
|---|---|
| pill | カテゴリの**文字色**を面に ･ 白文字 |
| マーカー | 面を不透明度30% ･ 平行四辺形 ･ 文字はカテゴリの文字色を太字 |
| カード ･ タイル | 地 ･ 本文は `#434343` |
| 番号カード | カテゴリの**文字色**を面に ･ 白文字 ･ 番号を大きく、名前を小さく |

#### マーカーのつくり方

1. マーカーは平行四辺形 ( 高さを低くしたもの )。
   面色を不透明度 30% にする
2. 文字の下半分に重ねる ( 上半分は出す )
3. 文字の左右に少しはみ出させる
4. 文字はカテゴリの文字色を太字

1スライドに2〜3箇所まで。
帯を敷かずに文字色だけで強調してもよい。
カテゴリ色のほかに、基調 ( Primary ) のマーカー `#C9D3E4` も使える ( §1.2 )。
カテゴリを割り当てていない資料の強調は、こちらを既定にする。

#### パターン

| パターン | 構成 |
|---|---|
| A ･ 番号カード | 左に文字色ベタの正方形 ( 番号 ＋ 名前 )、右に本文 |
| B ･ タイル一覧 | 地色のタイルに文字色。枠線も影も付けない <!-- design-check: allow NO_PERIOD_LINEBREAK --> |
| C ･ 見出し帯と引用 | 見出し帯は文字色ベタ、カードの地は地色、引用は白のボックスで抜く |
| D ･ pill と矢印 | pill は文字色に白文字。行は本文色のまま <!-- design-check: allow NO_PERIOD_LINEBREAK --> |
| E ･ pill を重ねる | pill をカードの上端に半分かける |

2軸4象限マップを色分けする場合は §2.5。

### 5.4 背景と地色

汎用スライドの地色は `#F0F5FC` の不透明度 85%。
白背景では `#F3F7FC` になる。

半透明にしておくと、下に画像やイラストがあっても地色として成立する。
不透明度を変えれば1色で濃さを調整できる。
白背景に置いたときの値は次のとおり。

| 不透明度 | 白背景での値 |
|---|---|
| 100% | `#F0F5FC` |
| 85% | `#F3F7FC` |
| 60% | `#F7FAFD` |
| 40% | `#FAFCFE` |

地色はすべて白に近いので、文字は `#434343` のまま置ける ( `#F3F7FC` の上で比 9.20 )。

### 5.5 Googleスライドのテーマ配色

スライド → テーマを編集 → 色 で設定する。
グラフを挿入したときに自動で当たるのはアクセント1から順なので、並び順が実質のスイッチになる。

| 枠 ( Googleスライド ) | pptx の枠 | 入れる色 | トークン |
|---|---|---|---|
| テキストと背景 1 | テキスト1 ( dk1 ) | `#434343` | スライド本文色 |
| テキストと背景 2 | 背景1 ( lt1 ) | `#FFFFFF` | Surface |
| テキストと背景 3 | テキスト2 ( dk2 ) | `#5A74A3` | Primary Dark |
| テキストと背景 4 | 背景2 ( lt2 ) | `#F7F7F7` | Background |
| アクセント 1 | accent1 | `#5A74A3` | Primary Dark |
| アクセント 2 | accent2 | `#C4A55A` | Accent |
| アクセント 3 | accent3 | `#9BB0D0` | Primary Light |
| アクセント 4 | accent4 | `#84B5B2` | CAT-2 青緑 |
| アクセント 5 | accent5 | `#A4A5D1` | CAT-1 紫 |
| アクセント 6 | accent6 | `#E0E0E0` | Border |
| リンク | hlink | `#5A74A3` | Primary Dark |
| 表示済みリンク | folHlink | `#9BB0D0` | Primary Light |

現在は既定のまま ( テキスト1 `#000000`、アクセント1 `#4285F4` ほか ) なので、全枠を入れ替える。 <!-- design-check: allow NO_TEXT_BLACK -->

#### 入れない色

- Point `#CD5151` … 自動で系列に当たると「ここを見て」の意味が壊れる。
  カスタム色として手で使う
- CAT-3 水色 `#98BBDE` ･ CAT-4 山吹 `#EDB369` ･ CAT-5 ローズ `#CB6378` … 案件ごとに変える層をテーマに焼き込むと「変えてよい色」が固定される。
  水色は Primary Light と ΔE 5.5 で系列としても見分けにくい

#### 入れ替えたあとの確認

1. 新しいスライドに 挿入 → グラフ → 縦棒 を置く。
   系列1が `#5A74A3`、2が `#C4A55A` になっていれば設定できている
2. 既存スライドで手塗りしていた `#6E87B6` などは、テーマ色ではなくカスタム色として残るので変化しない <!-- design-check: allow NO_HARDCODED_PRIMARY -->

### 5.6 スプレッドシートのテーマ配色

リンクされたグラフはスライドのテーマ配色に従わない。
スプレッドシート側にも同じ配色を設定する必要がある。

表示形式 → テーマ → カスタマイズ で設定する。

| 枠 | 入れる色 | トークン |
|---|---|---|
| テキスト | `#434343` | スライド本文色と揃える |
| 背景 | `#FFFFFF` | Surface |
| グラフの色 1 | `#5A74A3` | Primary Dark |
| グラフの色 2 | `#C4A55A` | Accent |
| グラフの色 3 | `#9BB0D0` | Primary Light |
| グラフの色 4 | `#84B5B2` | CAT-2 青緑 |
| グラフの色 5 | `#A4A5D1` | CAT-1 紫 |
| グラフの色 6 | `#E0E0E0` | Border |
| ハイパーリンク | `#5A74A3` | Primary Dark |

並び順はスライドのアクセント1〜6と同じにする。
揃えておかないと、同じ資料の中でスライド作成のグラフとスプレッドシート由来のグラフで色が食い違う。

#### 運用

- **作成済みのグラフには遡及しない**。
  既存分はグラフ側で個別に色を入れ直す
- 分析担当に配るときは、配色済みの空スプレッドシートを1つ作って「コピーして使う」形にするのが確実
- 条件付き書式のカラースケールは既定が `#F3F3F3` → `#E67C73` の赤。
  §2.2 の濃淡に差し替える

### 5.7 3カラムレイアウト

コンテンツ幅を3等分する。
値は実際のスライドから測ったもので、§5.2 のコンテンツエリアに収まる。

| 項目 | 値 |
|---|---|
| コンテンツ幅 | 9.21 in ( x 0.39 → 9.60 ) |
| カード幅 | 2.94 in |
| ギャップ | 0.20 in ( 目安 ) |
| カードの地 | `#F3F7FC` |
| カード内の見出し | Primary ･ 16pt ･ 太字 |
| カード内の本文 | `#434343` ･ 14pt ( 補足は 11pt ) |
| 枠線 ･ 影 | 影はなし。地色だけで面を作るのが基本形。白背景の角丸囲み ( §1.3 ) も使えるが、Web UI 感が強くなるので推奨はしない <!-- design-check: allow NO_PERIOD_LINEBREAK --> |

2カラムならギャップ 0.24、4カラムなら 0.16 に狭める。

#### 型

| 場面 | やり方 |
|---|---|
| 3つが並列 | 地色カード ＋ 中央見出し。色は3つとも同じ <!-- design-check: allow NO_PERIOD_LINEBREAK --> |
| 3つに順序がある | 番号カードを付ける。色は変えない <!-- design-check: allow NO_PERIOD_LINEBREAK --> |
| 3つが別のカテゴリ | 見出し帯にカテゴリの文字色、地にカテゴリの地色 |
| 3つの数値を見せる | ドーナツを3つ。向き ･ 太さ ･ 色を揃え、値だけ変える <!-- design-check: allow NO_PERIOD_LINEBREAK --> |
| 1つが主役 | 残り2つは地を `#F7F7F7`、文字を `#666666` に落とす |
| 出典が要る | カード内に細い罫線を引いて下に置く |

#### やらないこと

- 3つのカードで高さを変える ( 内容量が違っても、地の高さは揃える )
- カードに枠線と影を両方付ける
- 並列の3つにカテゴリの3色を割り当てる ( 並列は同色。
  色分けは「別のもの」の合図 )
- ギャップを 0.20 in より広げて、カード幅を削る

### 5.8 装飾の線

角丸のカードの上や横に色帯を敷いて、色を付けただけの状態にしない。

| 使い方 | 扱い |
|---|---|
| テンプレートに元からある縦棒 ( タイトル左 `#7288B2` ) | 残す。版面の一部 <!-- design-check: allow NO_PERIOD_LINEBREAK --> |
| 見出しのデザインとして組み込まれた線 | 可 |
| 白背景の角丸囲み ( `#E0E0E0` ) | 可 ( §1.3 ) |
| カードごとに反復する左3px ･ 上3px の色帯 | 使わない |
| 消しても意味が変わらない線 | 使わない |

階層は線ではなく、**面 ･ 余白 ･ 文字サイズ ･ 番号** で作る ( 作り方は §3 )。
実測では、これら4つですでに4段ぶんの差がついているところに
左の色帯が5つ目の合図として重なっていた。
情報としては重複している。

装飾としてのボーダーが一切禁止というわけではない。
白背景で見せたい場面の角丸囲みは可 ( §1.3 )。
それ以外では積極的には使わない。

---

## 未確定の観察項目

- Layer 2 の彩度条件 ( C\* の差 35 以内 ) は、定番セットが収まる範囲から逆算した値。
  運用しながら詰める
- 山吹 `#EDB369` と Warning `#F0AD4E` が ΔE 3.6 で見分けがつかない。
  旧山吹 `#F4AD56` ( ΔE 2.1 ) を警告色から離す目的で `#F4B56C` ( ΔE 4.1 ) に調整した経緯があるが、
  実運用値 `#EDB369` に戻したことで元の問題が戻っている。
  **できれば2色を別々にしたい**方向だが、どちらをどう動かすかは保留。
  当面は「山吹はカテゴリ、Warning は状態表示」と用途で分け、同じ図の中に並べない
- `figure.html` ( 図表のプレビュー ) は未着手。
  スライドの見本は pptx が担うため、必要になってから足す
- §3.1 の 1「左に番号を置く」の番号の色は決めていない。
  スライドの番号カードは §5.3 のとおりカテゴリの文字色ベタだが、
  見出しの番号に同じ扱いを当てるかは未定
- §3 の Web への適用は `figure.html` を出してから詰める。
  それまでは §3.2 の1行にとどめる
- テンプレートの章番号 `#90BBE1` が CAT-3 水色 `#98BBDE` と ΔE 1.8。
  章扉と本文ページなので同時には出ないが、
  テンプレート側が水色に近い値を持っている状態にはある
- 2軸4象限の領域の円 `#E6EBF7` は白との比 1.19 で、面の下限 1.2 をわずかに割る。
  実運用値なのでそのまま使っている
- 基調のマーカー `#C9D3E4` は紫 `#DDDEEE` と ΔE 5.0、水色 `#D9E6F3` と ΔE 4.9。
  段として並べる下限 ( ΔE 4 ) は満たすが余裕は小さい。
  同じ図の中で3つを並べない

---

## 別フェーズに送るもの

このファイルの適用範囲 ( 図表とスライド ) に収まらないもの。
`DESIGN.md` を触らない前提なので、ここには入れない。

### HTMLレポートのページ設計

オーナーは txtToHTML の `specs/report-html-spec-v2.md` と `docs/決定ログ.md`。
このファイルを上流として参照する形にする。

- 見出しの階層 ( ページタイトル ･ セクション )
- ナビゲーション ･ タブ ･ 1画面のタップ可能要素の数
- クリックできない要素への影とホバー装飾
- 段落の長さ ( 実測 : Notion 38字 に対し HTML出力 65字 ) と強調の密度 ( Notion 1.78件/段落 に対し 0.00 〜 0.49件 )

### Web UI コンポーネント

`DESIGN.md` §4 Component Stylings の範囲。
現在は Buttons ･ Inputs ･ Cards の3つのみで、表 ･ タブ ･ バッジなどの定義がない。
`DESIGN.md` を触る話になるため、別フェーズで扱う。
